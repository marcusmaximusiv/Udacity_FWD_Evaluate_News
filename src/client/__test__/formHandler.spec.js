import { formHandler } from "../js/formHandler"

//this section of the code to see if the formHandler is working 
describe("Testing to see if the form submission is working properly", () => {
    test("Testing the formHandler() function", () => {
           expect(formHandler).toBeDefined();
})});